import type { SidebarNavItem } from '@/components/types'

export const SIDEBAR_LINKS: SidebarNavItem[] = [
  {
    title: 'MENU',
    items: [
      {
        href: '/',
        icon: 'folder',
        title: 'Projects',
      },
    ],
  },
]
